/** Left inset reserved for the floating coach nav rail — keep AppShell /
 * CoachBottomChatBar padding in sync so content never slides under the icons. */
export const COACH_NAV_CONTENT_INSET_CLASS = "pl-24";
